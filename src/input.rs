#[derive(Debug)]
pub enum Input {
    A,
    B,
    Left,
    Right,
    Up,
    Down,
    Start,
    Select,
}

// #[cfg(test)]
// mod tests {
//     use super::*;

//     #[test]
// fn test_create_gb() {
// let country: Country = get_countries("SE").ok().unwrap();
// assert_eq!(country.language_code, "sv");
// }
// }

